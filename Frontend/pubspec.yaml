name: smart_learn
description: Application d'apprentissage intelligent avec IA

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.6

  # Requêtes HTTP
  http: ^1.1.0

  # Stockage local
  shared_preferences: ^2.2.2
  flutter_dotenv: ^5.1.0

  # State Management (optionnel, peut être utile plus tard)
  # provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env

  # Si vous avez des assets
  # assets:
  #   - assets/google_logo.png
  #   - assets/images/

  # Fonts personnalisées (optionnel)
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #       - asset: fonts/Roboto-Bold.ttf
  #         weight: 700